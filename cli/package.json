{
  "name": "arc-agent",
  "version": "0.1.0",
  "description": "CLI for spawning and managing Arc Agents",
  "bin": {
    "arc-agent": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --watch",
    "start": "node dist/index.js",
    "lint": "eslint src/"
  },
  "keywords": [
    "arc",
    "agent",
    "cli",
    "x402"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@arc-agent/sdk": "*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "inquirer": "^9.2.0",
    "conf": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/inquirer": "^9.0.7",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
